// The prompt library. One entry per video: what the page promises, and what
// the confirmation email delivers. Entries are appended below the marker by
// tools/new-prompt-page.py, so the file stays machine-editable.

const PROMPTS = {};

module.exports = { PROMPTS };

// <<entries>>
